<EditForm Model="Text" OnValidSubmit="OnValidSubmit">
    <DataAnnotationsValidator />

    <div class="form-group">
        <label>Please enter text in text field below:</label>
        <div>
            <InputText class="form-control" @bind-Value="@Text.Content" />
            <ValidationMessage For="@(() => Text.Content)" />
        </div>
    </div>
    <button class="btn btn-primary" type="submit">@ButtonName</button>
</EditForm>

@code {
    [Parameter] public Text Text { get; set; }

    [Parameter] public string ButtonName { get; set; } = "Submit";

    [Parameter] public EventCallback OnValidSubmit { get; set; }
}
